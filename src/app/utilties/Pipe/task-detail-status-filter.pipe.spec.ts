import { TaskDetailStatusFilterPipe } from './task-detail-status-filter.pipe';

describe('TaskDetailStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDetailStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
