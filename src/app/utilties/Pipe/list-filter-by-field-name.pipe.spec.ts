import { ListFilterByFieldNamePipe } from './list-filter-by-field-name.pipe';

describe('ListFilterByFieldNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ListFilterByFieldNamePipe();
    expect(pipe).toBeTruthy();
  });
});
